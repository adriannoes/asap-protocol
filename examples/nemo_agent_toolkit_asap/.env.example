# Optional — only for full `nat run` react_agent path (not required for ASAP-side smoke).
# Copy to .env locally; never commit real secrets. Placeholders only.
#
# NVIDIA_API_KEY=replace-me

# Dev-only ASAP Agent JWT override (normally asap_mcp_server.py mints and injects this).
# Unsafe for multi-tenant production — see docs/guides/automation-connector-security.md
# and docs/adapters/mcp-auth-bridge.md (allow_env_jwt_fallback).
# ASAP_AGENT_JWT=replace-me
