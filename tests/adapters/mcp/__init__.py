"""Tests for the MCP Auth Bridge (now :mod:`asap.mcp.auth`).

These tests exercise the canonical ``asap.mcp.auth`` implementation. The
``asap.adapters.mcp`` deprecation shim is verified separately by the
import-resolution check.
"""
