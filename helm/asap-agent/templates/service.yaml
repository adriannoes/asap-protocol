apiVersion: v1
kind: Service
metadata:
  name: {{ include "asap-agent.fullname" . }}
  labels:
    {{- include "asap-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "asap-agent.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
